services:
  - type: web
    name: sgi-front
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: VITE_API_URL
    staticPublishPath: dist
    healthCheckPath: /